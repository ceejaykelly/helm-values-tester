## Integrate Into Go Tests Use Case

## Overview

This example demonstrates how to integrate YAML merging and assertion logic directly into your Go test suite. By using the ya libraries programmatically, you can validate Helm values files and configuration overrides as part of your standard CI or local tests—catching errors early and ensuring your YAML produces the expected results before deployment.